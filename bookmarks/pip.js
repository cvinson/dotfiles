javascript:[   document,   ...[...document.querySelectorAll("iframe")]     .map(iframe => iframe.contentDocument)     .filter(iframe => !!iframe) ].some(d =>   [...d.querySelectorAll("video")]     .filter(video => video.paused == false && video.ended == false)    .map((video) => { video.disablePictureInPicture = false; return video; })     .some(       video => !!video.requestPictureInPicture().catch(err => console.log(err))     ) );
